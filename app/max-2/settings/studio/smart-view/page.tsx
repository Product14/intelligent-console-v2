'use client';

import { ComingSoon } from '@/components/settings/shell/coming-soon';

export default function StudioSmartViewPage() {
  return (
    <ComingSoon
      title="Studio AI — Smart View"
      description="The 360° spin viewer embedded on your vehicle detail pages, with an optional Vini call-to-action overlay."
      detail="This screen will be available once Studio AI is set up for this rooftop."
    />
  );
}
