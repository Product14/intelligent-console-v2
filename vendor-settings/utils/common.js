function _instanceof(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return left instanceof right;
    }
}
function _instanceof1(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof(left, right);
    }
}
function _instanceof11(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1(left, right);
    }
}
function _instanceof111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11(left, right);
    }
}
function _instanceof1111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111(left, right);
    }
}
function _instanceof11111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111(left, right);
    }
}
function _instanceof111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111(left, right);
    }
}
function _instanceof1111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111(left, right);
    }
}
function _instanceof11111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111(left, right);
    }
}
function _instanceof111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111(left, right);
    }
}
function _instanceof1111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111(left, right);
    }
}
function _instanceof11111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111(left, right);
    }
}
function _instanceof111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111(left, right);
    }
}
function _instanceof1111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111(left, right);
    }
}
function _instanceof11111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111(left, right);
    }
}
function _instanceof111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111(left, right);
    }
}
function _instanceof1111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111111(left, right);
    }
}
function _instanceof11111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111111(left, right);
    }
}
function _instanceof111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111111(left, right);
    }
}
function _instanceof1111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111111111(left, right);
    }
}
function _instanceof11111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111111111(left, right);
    }
}
function _instanceof111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111111111(left, right);
    }
}
function _instanceof1111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111111111111(left, right);
    }
}
function _instanceof11111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111111111111(left, right);
    }
}
function _instanceof111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111111111111(left, right);
    }
}
function _instanceof1111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111111111111111(left, right);
    }
}
function _instanceof11111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111111111111111(left, right);
    }
}
function _instanceof111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111111111111111(left, right);
    }
}
function _instanceof1111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111111111111111111(left, right);
    }
}
function _instanceof11111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111111111111111111(left, right);
    }
}
function _instanceof111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111111111111111111(left, right);
    }
}
function _instanceof1111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111111111111111111111(left, right);
    }
}
function _instanceof11111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111111111111111111111(left, right);
    }
}
function _instanceof111111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111111111111111111111(left, right);
    }
}
function _instanceof1111111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111111111111111111111111(left, right);
    }
}
function _instanceof11111111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111111111111111111111111(left, right);
    }
}
function _instanceof111111111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111111111111111111111111(left, right);
    }
}
function _instanceof1111111111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof111111111111111111111111111111111111(left, right);
    }
}
function _instanceof11111111111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof1111111111111111111111111111111111111(left, right);
    }
}
function _instanceof111111111111111111111111111111111111111(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else {
        return _instanceof11111111111111111111111111111111111111(left, right);
    }
}
function _type_of(obj) {
    "@swc/helpers - typeof";
    return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj;
}
export var localStorageKeys = {
    AUTHKEY: 'authKey',
    DEVICEID: 'deviceId',
    DEFAULTBEARERTOKEN: 'defaultBearerToken',
    USERDETAILS: 'userDetails',
    defaultEnterprise: 'defaultEnterprise',
    permissionObject: 'permissionObject',
    onBoardingDetails: 'onboardingDetails',
    downloadPollingId: 'downloadPollingId',
    downloadRecords: 'downloadRecords',
    videoRecords: 'videoRecords',
    epConfig: 'epConfig',
    reportImagesModal: 'reportImagesModal',
    showWarningModal: 'showWarningModal',
    guestLogin: 'guestLogin',
    processCount: 'processCount',
    src: 'src',
    rememberChoiceVS: 'rememberChoiceVS',
    rememberChoiceVirtual360: 'rememberChoiceVirtual360',
    VSProjectCount: 'VSProjectCount',
    Virtual360VideoCount: 'Virtual360VideoCount',
    countEnterpriseListIsGreaterThan1: 'countEnterpriseListIsGreaterThan1',
    firstTimeUser: 'firstTimeUser',
    coachMarkCompletedSteps: 'coachMarkCompletedSteps',
    inventoryCoachMarkCompletedSteps: 'inventoryCoachMarkCompletedSteps',
    enterpriseSequenceIsActive: 'enterpriseSequenceIsActive',
    ipApiJson: 'ipApiJson',
    enterpriseTopUpCount: 'enterpriseTopUpCount',
    websiteOnboradingSteps: 'websiteOnboradingSteps',
    templateModalSync: 'templateModalSync',
    syncStatusData: 'syncStatusData',
    websiteOnboardingDone: 'websiteOnboardingDone',
    partnerCoachmarks: 'partnerCoachmarks',
    vsVersion: 'vsVersion'
};
export var sessionStorageKeys = {
    TEAMID: 'teamIds',
    selectedEnterprise: 'selectedEnterprise',
    selectedTeam: 'selectedTeam',
    enterpriseReasons: 'enterpriseReasons',
    teamList: 'teamList',
    usersList: 'usersList',
    makeLists: 'makeLists',
    studioData: 'studioData',
    studioDataWall: 'studioDataWall',
    isWallEnabled: 'isWallEnabled',
    enterpriseList: 'enterpriseList',
    guestUserId: 'guestUserId',
    skuProcessed: 'skuProcessed',
    enterprisePlan: 'enterprisePlan',
    enterpriseFeatures: 'enterpriseFeatures',
    stepCreateEnterprise: 'stepCreateEnterprise',
    activeTabInProjectsPage: 'activeTabInProjectsPage',
    onboardingAccountDetails: 'onboardingAccountDetails',
    actualEnterpriseData: 'actualEnterpriseData',
    credit: 'credit',
    requestedCredits: 'requestedCredits',
    selectedTabHome: 'selectedTabHome'
};
export var capitalize = function capitalize(str) {
    if (typeof str === 'string' && str.length > 0) {
        return str[0].toUpperCase() + str.slice(1);
    }
    return '';
};
export function removeSpecialCharactersAndSpaces(str) {
    var cleanedStr = str === null || str === void 0 ? void 0 : str.replace(/[^\w\s]/gi, '').replace(/\s+/g, '');
    var lowerCaseStr = cleanedStr === null || cleanedStr === void 0 ? void 0 : cleanedStr.toLowerCase();
    return lowerCaseStr;
}
export function deepEquals(a, b) {
    if (a === b) return true;
    if (a == null || b == null) return false;
    if (_instanceof111111111111111111111111111111111111111(a, Date) && _instanceof111111111111111111111111111111111111111(b, Date)) return a.getTime() === b.getTime();
    if (_instanceof111111111111111111111111111111111111111(a, RegExp) && _instanceof111111111111111111111111111111111111111(b, RegExp)) return a.toString() === b.toString();
    if ((typeof a === "undefined" ? "undefined" : _type_of(a)) !== 'object' || (typeof b === "undefined" ? "undefined" : _type_of(b)) !== 'object') return false;
    var keysA = Object.keys(a);
    var keysB = Object.keys(b);
    if (keysA.length !== keysB.length) return false;
    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
    try {
        for(var _iterator = keysA[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
            var key = _step.value;
            if (!keysB.includes(key)) return false;
            if (!deepEquals(a[key], b[key])) return false;
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally{
        try {
            if (!_iteratorNormalCompletion && _iterator.return != null) {
                _iterator.return();
            }
        } finally{
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }
    return true;
}
